<?php

if (! function_exists('config')) {
    /**
     * Esta función emula únicamente la carga de parámetros de conexión a la base de datos
     * de la librería Illuminate, con el fin de ser compatible con Laravel y otros proyectos similares
     */
    function config()
    {
    }
}
